# HabitFlow Backend

Backend package for HabitFlow.

## Structure

- `src/habitflow/` — core backend package
- `test/` — backend tests

## Development

Run project tasks from workspace root using Pixi:

- `pixi run test-backend`
- `pixi run lint-backend`
- `pixi run typecheck-backend`
- `pixi run build-backend`
